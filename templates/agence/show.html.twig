{% extends 'base.html.twig' %}

{% block title %}Agence{% endblock %}

{% block body %}
    <h1>Agence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agence.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ agence.Nom }}</td>
            </tr>
            <tr>
                <th>Num_Tel</th>
                <td>{{ agence.NumTel }}</td>
            </tr>
            <tr>
                <th>Adress_Ville</th>
                <td>{{ agence.AdressVille }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('agence_index') }}">back to list</a>

    <a href="{{ path('agence_edit', {'id': agence.id}) }}">edit</a>

    {{ include('agence/_delete_form.html.twig') }}
{% endblock %}
